# Test/CI blast-radius audit (read-only)

Planned moves:
1. `create-midas/` â†’ `cli/`
2. Engine dogfood root `.harness/` (evidence: retros, sweeps, audits) â†’ `runs/`

**Scope:** `scripts/test.mjs`, `scripts/ci-install-smoke.mjs`, `scripts/precommit-eval.mjs`, `.github/workflows/*.yml`

---

## Executive summary

| Scenario | Outcome |
|----------|---------|
| **Both git mv, zero test/CI edits** | `node scripts/test.mjs` **crashes at load** (`ERR_MODULE_NOT_FOUND` on `../create-midas/migrate-v2.mjs` line 39). **0** `check()` results; CI fails on step 1. |
| **Only `create-midas` â†’ `cli/`** (if import fixed but paths not) | Second **uncaught ENOENT** at line 1168 (`readFileSync(join(ROOT, 'create-midas', 'index.mjs'))`). Still no graceful check tally. |
| **Only engine `.harness` â†’ `runs/`** | Suite runs; **3 failures** today (`dogfood:retros:retro-01/02/03`). `paths:classic-runs` still passes until `harness/state.yaml` `paths.runs` is updated. |
| **Both moves + full path updates** | **~85â€“110+** named `check()` names in scoped files, **~33** dynamic `create-template:has:*`, **6** CI workflow steps, **1** host-matrix smoke script. |

`scripts/test.mjs` alone has **96** `join(ROOT, 'create-midas', â€¦)` and **104** `create-midas` string hits.

---

## 1) Fatal blockers (git mv only)

| Location | Pattern | Effect |
|----------|---------|--------|
| `scripts/test.mjs:39` | `from '../create-midas/migrate-v2.mjs'` | Module load failure |
| `scripts/test.mjs:1168â€“1171` | `readFileSync(join(ROOT, 'create-midas', â€¦))` | Uncaught ENOENT (no guard) |
| `scripts/test.mjs:444` | `execSync('node scripts/build-create.mjs')` | `build-create.mjs` still targets `create-midas/template` |
| `package.json` | `"files": ["create-midas"]`, `"midas": "create-midas/index.mjs"` | `npm pack` / bin broken |
| `.github/workflows/ci.yml` | All `create-midas/â€¦` invocations | 5 smoke steps fail |

---

## 2) Named checks to update (by prefix)

### `create-midas:*` (10 in `test.mjs`)

| Check name | What it asserts |
|------------|-----------------|
| `create-midas:pkg:bin` | `pkg.bin['create-midas'] === 'index.mjs'` |
| `create-midas:pkg:type` | `type === 'module'` |
| `create-midas:pkg:files` | Exact files array |
| `create-midas:pkg:engine-floor` | `engines.node === '>=22'` |
| `create-midas:pkg:homepage` | Homepage URL |
| `create-midas:pkg:repository-url` | Repo URL |
| `create-midas:pkg:repository-dir` | **`directory === 'create-midas'`** |
| `create-midas:pkg:bugs` | Bugs URL |
| `create-midas:pkg:keywords` | Keyword list |
| `create-midas:pkg:json` | Parse error path |
| `create-midas:files-install-diagnose` | Reads `create-midas/package.json` |

Also: `version:create-midas/package.json` (dynamic `version:${f}` loop, line 833).

### `installer:*` (~52 named checks in `test.mjs`)

**Static (read `create-midas/` sources):**  
`installer:fillAgents-paths-arg`, `installer:no-bare-readToolsFromState`, `installer:tools-flag`, `installer:tool-onboarding`, `installer:no-root-gemini-extension`, `installer:tools-presets`, `installer:tty-fallback`, `installer:update-honours-tools`, `installer:sync-skill-mirrors`, `installer:default-tools-cursor`, `installer:lib-workflow-engine`, `installer:prune-orphan-adapters`, `installer:ops-runner`, `installer:thin-shim`, `installer:runner-skips-informational-ops`, `installer:tools-vocabulary` (Ã—2 branch), `installer:ensure-gitignore`, `installer:gitignore-merge`, `installer:gitignore-report-always`, `installer:gitignore-after-engine`, `installer:verify-after-update`, `installer:verify-auto-fix-routing`, `installer:update-complete-hint`, `installer:layout-flag`, `installer:refuses-engine-repo`, `installer:refuses-engine-repo-runtime`, `installer:hasMidasInstall-compact`, `installer:engine-owns-lifecycle`, `installer:bind-applies`, `installer:diagnose-flag`, `installer:compare-versions`, `installer:detect-context-empty`, `installer:plan-topo-sort` (Ã—2), `installer:plan-missing-dep` (Ã—2), `installer:plan-cycle` (Ã—2)

**Subprocess fixtures (spawn `create-midas/index.mjs`):**  
`installer:rollback-restores`, `installer:update-conflict-fixture-install`, `installer:update-stale-manifest-install`, `installer:update-stale-manifest-rebaseline`, `installer:update-tools-fixture-install`, `installer:update-tools-rewrites-and-prunes`, `installer:project-rules-fixture-install`, `installer:uninstall-fixture-install`, `installer:uninstall-respects-ownership`, `installer:nested-parent-install`, `installer:nested-install-refuses`, `installer:uninstall-dry-fixture`, `installer:uninstall-dry-run`, `installer:uninstall-purge`, `installer:upgrade-fixture`, `installer:update-dry-conflict-fixture`, `installer:update-dry-run-reports-vendor-conflict`

**Related (not `installer:` prefix):**  
`diagnose:cli:runs`, `diagnose:json-envelope`, `diagnose:not-installed`, `diagnose:setup-pending`, `diagnose:missing-path-is-read-only`, `diagnose:matrix-*` (6), `autonomy:installer-flag`, `autonomy:install-*` (4), `mcp:installer-wraps-npx-on-windows`, `mcp:installer-preserves-user-config`

### `build-create:*` / `create-template:*` (~48+ in `test.mjs`)

**`build-create:`**  
`build-create:excludes-harness-state-yaml`, `build-create:conventions-match`, `build-create:harness-tree-match`, `build-create:excludes-harness-trace-research`, `build-create:autonomy-optional-only`, `build-create:portable-skills-match`, `build-create:scripts-tree-match`, `build-create:install-diagnose`

**`create-template:`** (paths under `cli/template/.harness/` â€” **product install layout, keep `.harness`**)  
`create-template:skills-match`, `create-template:engine-excludes-plugins`, `create-template:excludes:harness/state.yaml`, `create-template:schema:no-mojibake`, `create-template:claude-tree-match`, `create-template:excludes-midas-precommit`, `create-template:agents-md:match`, `create-template:portable-skills:match`, `create-template:install-diagnose:match`, `create-template:design-system-script:match`, `create-template:gemini-extension:match`, `create-template:agents-and-models:match`, `create-template:skill-quality-gate:match`, `create-template:skill-flows`, `create-template:skill-flows:match`, `create-template:stage-command-table:match`, `create-template:design-system:tokens-css:match`, `create-template:gates-index:match`, `create-template:checks-index:json`, `create-template:checks-index:match`, `create-template:skill-registry:match`

**Dynamic (33 checks):** `create-template:has:${f}` for each path in the loop at line 404 (all under `tplRoot`).

**Exclude rule:** `create-template:excludes:create-midas` â†’ should become `create-template:excludes:cli` (line 408).

### `paths:classic-runs` and related (engine dogfood only)

| Check | Current expectation | After `.harness` â†’ `runs/` |
|-------|---------------------|------------------------------|
| **`paths:classic-runs`** | `classic.runs === '.harness'` | **FAIL** once `harness/state.yaml` has `paths.runs: runs` |
| `paths:classic-product` | `docs/product` (from state override) | Unchanged |
| `paths:migration-map-sprints` | `MIGRATION_MAP` has `from: '.harness/sprints'` | **Product/legacy** â€” do **not** change for engine dogfood rename |
| **`dogfood:retros:retro-01/02/03`** | `join(ROOT, '.harness', 'retros', â€¦)` | **FAIL** (3) on git mv alone |

### `engine-repo:*` / `layout:*`

| Check | Blast from these moves |
|-------|------------------------|
| **`engine-repo:no-nested-product-install`** | Uses `join(ROOT, '.harness', 'engine/VERSION')` and `state.yaml`. After dogfood mv, **passes vacuously** â€” should assert **absence of product install markers** under new `runs/` or keep checking `.harness/` only if product layout unchanged. |
| `installer:refuses-engine-repo` | Source grep in `cli/lib/â€¦` â€” path update only |
| `installer:refuses-engine-repo-runtime` | Spawns `cli/index.mjs --dry-run ROOT` |
| **`layout:*` (6 in test.mjs)** | **Unaffected** by renames (`layout:no-legacy-*`, `layout:research-taskpilot-fixture`, `layout:engine-marketplace-json`, `layout:engine-product-fixture`) |
| `paths:compact-*`, `paths:hub-*`, `paths:detect-*` | Unaffected |

### `npm pack` greps

| File | Pattern | Update to |
|------|---------|-----------|
| `.github/workflows/ci.yml:97` | `npm pack --dry-run 2>&1 \| grep -q create-midas` | `grep -q cli` (or new package name) |
| `package.json` | `"files": ["create-midas"]` | `"cli"` |
| `release-prep.yml:47` | `npm pack` (no grep) | Indirect â€” tarball contents depend on `files` |

No `npm pack` grep in `test.mjs` or `precommit-eval.mjs`.

---

## 3) Hardcoded `join(ROOT, 'create-midas'â€¦)` in scoped files

**`scripts/test.mjs`:** 96 occurrences (reads, imports, spawns, template paths).

**`scripts/precommit-eval.mjs`:** 2 paths + 1 exists guard:

```113:116:c:\Users\AfterMe\Desktop\Harness\scripts\precommit-eval.mjs
const tplSkill = join(ROOT, 'create-midas', 'template', '.harness', 'engine', 'skills', 'midas-precommit');
// ...
if (existsSync(join(ROOT, 'create-midas', 'template'))) {
  add('template-excludes-precommit', !existsSync(tplSkill), 'create-midas/template must omit midas-precommit');
```

**`scripts/ci-install-smoke.mjs`:** spawn args only (not `join`):

```27:27:c:\Users\AfterMe\Desktop\Harness\scripts\ci-install-smoke.mjs
  const install = run(['create-midas/index.mjs', `--tools=${host}`, target]);
```

(Product `.harness/` assertions in same file are **correct** â€” installed layout, not engine dogfood.)

---

## 4) `classic.runs === '.harness'` expectations

| File | Line | Notes |
|------|------|-------|
| `scripts/test.mjs` | 2117 | `check('paths:classic-runs', classic.runs === '.harness')` |
| `scripts/paths.mjs` | 23 | `CLASSIC.runs: '.harness'` default |
| `harness/state.yaml` | 10 | `paths.runs: .harness` override |

**Also related (not `classic.runs`, but same string):**

- `paths:migration-map-sprints` â€” legacy **product** migration map
- `bundle:canonical-v2-runs` â€” product v2 `.harness/runs/â€¦`
- `gitignore:snippet:volatile-hash` â€” product `.harness/*.hash`

Do **not** blanket-replace `.harness` in installer/template/migrate tests.

---

## 5) Product `.harness` vs engine dogfood `.harness` â€” mixing risks

| Location | Namespace | Action on engine `runs/` rename |
|----------|-----------|----------------------------------|
| `dogfood:retros:*` (1088) | **Engine dogfood** | **Update** â†’ `join(ROOT, 'runs', 'retros')` |
| `engine-repo:no-nested-product-install` (1416) | **Product install guard** at repo root | **Strengthen** â€” today passes after dogfood mv; should not conflate with `runs/` evidence |
| `layout:research-taskpilot-fixture` (419) | **Product fixture** `docs/research/taskpilot/.harness/` | **Leave** |
| `create-template:has:.harness/â€¦` (404 loop) | **Product install template** | **Leave** `.harness` |
| Installer subprocess fixtures (`updateRoot/.harness/â€¦`) | **Simulated product install** | **Leave** |
| `ci.yml` smoke (`/tmp/midas-smoke/.harness/â€¦`) | **Installed product** | **Leave** |
| `ci-install-smoke.mjs` `.harness/engine`, `state.yaml` | **Installed product** | **Leave** |
| `ownership:*`, `preserve:*`, `migrate-v2:*` | **Product path semantics** | **Leave** unless product layout changes |

**Misleading test today:** `engine-repo:no-nested-product-install` only looks for root `.harness/engine` + `.harness/state.yaml`. After dogfood evidence moves to `runs/`, it no longer proves â€œno nested product installâ€ unless you add checks for product markers under `runs/` or keep explicit negative assertions.

---

## 6) CI workflow checklist (`.github/workflows/ci.yml`)

| Step | Patterns to update |
|------|-------------------|
| create-midas template sync | `create-midas/template`, error message `build-create.mjs` |
| Installer smoke (cursor) | `node create-midas/index.mjs` |
| Installer smoke (multi-host) | same |
| Diagnose smoke | `node create-midas/index.mjs --diagnose` |
| Update smoke | `create-midas/index.mjs --update` |
| **npm pack dry-run** | `grep -q create-midas` |

**Unchanged in CI:** all `/tmp/midas-*//.harness/â€¦` paths (installed product).

**`host-smoke` job:** `ci-install-smoke.mjs` â€” update spawn path to `cli/index.mjs`.

**`release-prep.yml` / `docs.yml`:** no `create-midas` references in scoped search.

---

## 7) `precommit-eval.mjs` checklist

| Item | Update |
|------|--------|
| `tplSkill` path | `cli/template/.harness/engine/skills/midas-precommit` |
| `existsSync(â€¦/create-midas/template)` | `cli/template` |
| Detail string | `create-midas/template` â†’ `cli/template` |
| `engine-repo` check | **No path change** (uses `isEngineRepo()` / package name) |

---

## 8) Actionable update order

1. **Unblock load:** `test.mjs:39` import â†’ `../cli/migrate-v2.mjs`; `build-create.mjs` TEMPLATE path.
2. **Mechanical replace in scoped files:** `create-midas` â†’ `cli` (96+ joins, 104 string hits in `test.mjs`; CI; `ci-install-smoke.mjs`; `precommit-eval.mjs`).
3. **Package contract:** `create-midas:pkg:*`, `version:cli/package.json`, root `package.json` `files` + `bin`, `npm pack` grep.
4. **Engine dogfood only:** `harness/state.yaml` `paths.runs`, `paths:classic-runs`, `dogfood:retros:*`, revisit `engine-repo:no-nested-product-install`.
5. **Do not touch:** template `cli/template/.harness/â€¦`, installer fixtures, `ci.yml` product smoke `.harness` paths, `paths:migration-map-sprints`, `migrate-v2:*` product paths.
6. **Re-run:** `node scripts/build-create.mjs`, commit `cli/template`, then full CI matrix.

---

## 9) Failure count estimate (git mv only, no edits)

| Layer | Count |
|-------|-------|
| **Immediate crash** | 1 (import) â†’ entire suite |
| **If import fixed, before checks** | 1+ (uncaught `readFileSync` ~1168) |
| **Engine dogfood mv only** | **3** assertions (`dogfood:retros`) + **1** after state update (`paths:classic-runs`) |
| **Full rename with tests updated** | 0 from this audit scope (assuming `paths.mjs` + installer + template aligned) |
| **Full rename, tests NOT updated** | **~85â€“110+** named checks + **33** `create-template:has:*` + **~25+** installer subprocess checks + **6** CI steps + **18** host-matrix smokes |

[REDACTED]
