## What & why
<!-- One or two sentences: what does this change and why. -->

## Checklist
- [ ] Edited **sources only** (`.claude/`, `harness/`, `scripts/`, `docs/`, `cli/index.mjs`) — not generated trees (`harness/plugins/midas/`, `harness/.claude-plugin/`, `cli/template/`, `CLAUDE.md`, `.cursor/`, `.windsurf/`, `GEMINI.md`).
- [ ] Ran `npm run align` (or `npm run verify`) — all green.
- [ ] Artifacts are in **English**; commit follows **Conventional Commits**.
- [ ] DCO sign-off (`git commit -s`).

## Notes
<!-- Migration impact, follow-ups, screenshots. -->
