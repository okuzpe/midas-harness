# Sandbox findings — phases 5–6 + install reconcile

2026-08-31 · continuation of [phases 0–4](./2026-08-31-phases-0-4.md) (no reset — working copy already at `architecture_rules`) · skills=`define-conventions`, `plan-sprints`, `midas-reconcile`

## Setup

- Engine: `midas-harness` 3.0.1
- Isolation: reused `sandbox/example-product/` after capture+0–4; `env` exit 0; `name: sandbox-example`
- Trace: `start-run` `session_id=a21436796187` `run_id=1dd6a4fb0e56` · `MIDAS_TRACE_ROOT=C:\Users\AfterMe\Desktop\Harness\sandbox\example-product`
- Task model: `composer-2.5` (not `-fast`)
  - [define-conventions](661cacea-488b-4d32-a295-5221aa7eafa7)
  - [plan-sprints](18000516-72f4-43ca-8fe7-e8e09a00a678)
  - [midas-reconcile](14f310f3-f190-433c-a003-585665ae07eb) on `sandbox/example-install/`
- Grades (parent, after each pipeline Task):

```
MIDAS_SANDBOX_ORACLE: skill=define-conventions isolation=ok checks=19 pass=19 fail=0 verdict=pass
MIDAS_SANDBOX_ORACLE: skill=plan-sprints isolation=ok checks=19 pass=19 fail=0 verdict=pass
```

- `finish` exit 0 with the same session/run ids.
- Isolation hashes of `harness/skills` and `harness/rules` still matched the capture-reset baseline.

## Decision-flow log

- `[SANDBOX AUTO-DECISION] Design direction -> calm local paper-checklist / kitchen notepad; Things 3 + Apple Reminders assumed (confirm); anti-ref generic purple SaaS hero`
- `[SANDBOX AUTO-DECISION] Enforcement install -> no (do not write hooks/CI into the engine repo); state enforcement: disabled`
- `[SANDBOX AUTO-DECISION] Adapter re-render -> skipped (paths.scripts is engine scripts; doctor --fix would mutate harness)`
- plan-sprints: no AskQuestion; two thin sprints (Vite add/list → complete + localStorage).
- define-conventions wrote `folder-structure.md`, `design-direction.md` (`## Brand personality`, `## How it should feel (metaphor)`), `design-system.md` (`## Framework`), `gate-05.md`, `stage: sprint_planning`.
- plan-sprints wrote template roadmap headings, sprint **Tasks tables**, `features.json` (all `failing`), `gate-06.md`, `stage: sprint_execution`.
- midas-reconcile (install profile): `node .harness/scripts/install-diagnose.mjs` from `sandbox/example-install/` → Status **`setup_pending`**, next **`/midas-init`**. Not `partial_migrate`. `paths.engine=.harness/engine`. Read-only.

## Issues found

1. **fixture-limit** — Pipeline fixture `paths.scripts=../../scripts` means Phase 5 cannot honestly re-render adapters / run `doctor --fix` without touching engine source. Adapter step skipped; oracle does not require it.
2. **fixture-limit** — Design references and Vite pin remain cheap-model / assumed (confirm). Not a product design freeze.
3. **isolation-bug (mitigated)** — Cursor Task still has no cwd/env pin; Task read `sandbox-env.json`. Cannot close from this repo.

## Proposed improvements (not applied)

1. Cursor Task cwd + env pin.
2. Optional: a pipeline profile that vendors `.harness/scripts` so Phase 5 doctor can run inside the working copy without an install-profile hybrid.

## Harness analysis

Phases 5–6 procedure headings matched the templates the oracles pin. `/midas-reconcile` on `--profile install` is no longer stuck on `partial_migrate` (closes the 2026-08-30 fixture-limit). No new `harness-gap` to capture — local `install-diagnose.mjs` is human-readable; machine JSON remains the `npx … --diagnose --json` path in the skill, not a missing flag on the nested script.

```
MIDAS_SANDBOX_RESULT: skill=define-conventions,plan-sprints,midas-reconcile mode=all verdict=pass auto_decisions=3 isolation=ok
```
