# Sandbox findings — smoke (idea-intake + contextualize)

2026-08-31 · mode=`smoke` · skills=`idea-intake`, `contextualize`

## Setup

- Engine: `midas-harness` 3.0.1
- Isolation: `node scripts/sandbox-run.mjs reset` then `env` exit 0; fixture `name: sandbox-example`
- Trace: `start-run` `session_id=6fa1c5627ef7` `run_id=f24c3d85b736` · `MIDAS_TRACE_ROOT=C:\Users\AfterMe\Desktop\Harness\sandbox\example-product`
- Task model: `composer-2.5` (not `-fast`) · [idea-intake](7cbccd0a-673c-4c50-8956-87dcdc8edd93) · [contextualize](3da3bc2d-8b5c-4b89-9b90-38b00449a249)
- First Read (both Tasks): fixture `name=sandbox-example`
- Grades (parent, after each Task):

```
MIDAS_SANDBOX_ORACLE: skill=idea-intake isolation=ok checks=20 pass=20 fail=0 verdict=pass
MIDAS_SANDBOX_ORACLE: skill=contextualize isolation=ok checks=18 pass=18 fail=0 verdict=pass
```

- `finish` exit 0 with the same session/run ids (not `no-active-run`).

## Decision-flow log

- `[SANDBOX AUTO-DECISION] Confirm mode (greenfield | brownfield)? -> greenfield (fixture default; Chorechip is a new local web app with no existing codebase)`
- `[SANDBOX AUTO-DECISION] Who is the exact target user? -> One household member tracking chores solo on their own machine (seed: one person, local, no sharing)`
- `[SANDBOX AUTO-DECISION] What specific pain makes this worth building? -> Chores get lost without a dedicated list; spreadsheets/mental notes are friction (seed + Phase 0 observations)`
- `[SANDBOX AUTO-DECISION] What is the one measurable success metric? -> Add, complete, and list open chores in one localhost browser session without errors (seed: add/complete/list)`
- `[SANDBOX AUTO-DECISION] What are explicit MVP non-goals? -> No auth, sharing/multi-user, mobile, notifications, billing, or cloud data (seed idea)`
- idea-intake wrote `gate-00.md` and advanced `stage: contextualize`.
- contextualize wrote `## Contextualized`, `open-questions.md` (empty `### BLOCKING`), `gate-01.md`, `stage: market_research`.
- No nested Task / other-model builder. Isolation hashes of `harness/skills` and `harness/rules` matched reset.

## Issues found

1. **fixture-limit** — Seed `idea.md` is already a complete Phase-0 capture, so idea-intake’s live signal remains state + gate freeze, not blank-template capture. Same as 2026-08-30; not a skill bug.
2. **fixture-limit** — Phases 2–4 were not part of this smoke. Cheap-model judgment on Chorechip market/architecture is not a product go/no-go.
3. **isolation-bug (mitigated)** — Cursor Task still has no cwd pin and does not inherit `MIDAS_TRACE_ROOT`. Parent `start-run`/`finish` now round-trip (exit 0). Residual: Task-emitted `trace-write` still needs the env passed per subprocess.

## Proposed improvements (not applied)

1. Optional blank-idea seed only if we want to certify *capture*, not gate advance.
2. Second seed that is a real nested install (`paths.engine=.harness/engine`) for `/midas-reconcile` / `update`.
3. Cursor product: Task cwd + env pin — cannot close from this repo.

Harness gaps from the 2026-08-30 idea-intake run (gate-00 heading, AskQuestion, finish fail-closed) and the Phase 1–6 playbook/skill split were closed in engine source **before** this smoke, then verified here.

```
MIDAS_SANDBOX_RESULT: skill=idea-intake,contextualize mode=smoke verdict=pass auto_decisions=5 isolation=ok
```
