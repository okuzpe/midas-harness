# Sandbox findings — capture seed + phases 0–4

2026-08-31 · mode=`all` (reset `--profile capture`) · skills=`idea-intake`, `contextualize`, `market-research`, `business-plan`, `choose-architecture`

## Setup

- Engine: `midas-harness` 3.0.1
- Isolation: `node scripts/sandbox-run.mjs reset --profile capture` then `env` exit 0; fixture `name: sandbox-example`
- Pre-Task `grade --skill idea-intake` **failed** on `pitch-not-todo` + `raw-not-todo` (blank template) plus gate/stage — capture signal, not seed snapshot
- Trace: `start-run` `session_id=a21436796187` `run_id=00dffc3393af` · `MIDAS_TRACE_ROOT=C:\Users\AfterMe\Desktop\Harness\sandbox\example-product`
- Pointer: `{work}/.harness/cache/sandbox-env.json` (Cursor Task still has no cwd/env pin)
- Task model: `composer-2.5` (not `-fast`)
  - [idea-intake](e27fdf71-de8c-4d4f-ba6e-dbf9df1e9404)
  - [contextualize](4d1fa8ad-c7d7-4fad-a5b3-164a5716b62c)
  - [market-research](5163d79c-49f0-4d98-9d99-3b56376d00bb)
  - [business-plan](02430938-d0d1-4247-acb4-795311288ff7)
  - [choose-architecture](7ee1885d-a200-46c9-b5ab-a60974ce81fc)
- First Read (all Tasks): fixture `name=sandbox-example`
- Grades (parent, after each Task):

```
MIDAS_SANDBOX_ORACLE: skill=idea-intake isolation=ok checks=21 pass=21 fail=0 verdict=pass
MIDAS_SANDBOX_ORACLE: skill=contextualize isolation=ok checks=18 pass=18 fail=0 verdict=pass
MIDAS_SANDBOX_ORACLE: skill=market-research isolation=ok checks=22 pass=22 fail=0 verdict=pass
MIDAS_SANDBOX_ORACLE: skill=business-plan isolation=ok checks=23 pass=23 fail=0 verdict=pass
MIDAS_SANDBOX_ORACLE: skill=choose-architecture isolation=ok checks=22 pass=22 fail=0 verdict=pass
```

- `finish` exit 0 with the same session/run ids (not `no-active-run`).
- Nested install (separate tree): `reset --profile install` → `env --profile install` exit 0;
  `node cli/install-diagnose.mjs sandbox/example-install` → **`setup_pending`** (not `partial_migrate`);
  `paths.engine=.harness/engine`.

## Decision-flow log

- `[SANDBOX AUTO-DECISION] Confirm mode (greenfield | brownfield)? -> greenfield (new local app, no existing codebase)`
- `[SANDBOX AUTO-DECISION] Who is the target user? -> One household member tracking chores solo on their own machine (provided Chorechip answer)`
- `[SANDBOX AUTO-DECISION] What is the core problem? -> Chores get lost without a dedicated list; spreadsheets/mental notes are friction (provided Chorechip answer)`
- `[SANDBOX AUTO-DECISION] What is the primary success metric? -> Add, complete, and list open chores in one localhost browser session without errors (provided Chorechip answer)`
- `[SANDBOX AUTO-DECISION] What are explicit MVP non-goals? -> No auth, sharing/multi-user, mobile apps, notifications, billing, or cloud data (provided Chorechip answer)`
- `[SANDBOX AUTO-DECISION] Competitors: Todoist, TickTick, Google Tasks, paper/spreadsheet substitute (fixture comps; skipped Any.do to keep table focused).`
- `[SANDBOX AUTO-DECISION] Demand verdict: mixed — strong category demand, weak commercial niche for localhost-only toy vs free bundled defaults.`
- `[SANDBOX AUTO-DECISION] Go / no-go recommendation? -> GO with field validation deferred (desk demand mixed; founder is sandbox-lab; no interviews)`
- `[SANDBOX AUTO-DECISION] Human sign-off (name + date)? -> Reviewed by: sandbox-lab; Approved on: 2026-08-31`
- `[SANDBOX AUTO-DECISION] App shape -> one local web SPA / monolith (Recommended) — no second client.`
- `[SANDBOX AUTO-DECISION] Auth -> none (local-only, no accounts)`
- `[SANDBOX AUTO-DECISION] Persistence -> browser localStorage (no server DB)`
- `[SANDBOX AUTO-DECISION] Frontend -> Vite + vanilla HTML/JS — pin Vite 8.2.2 (web fallback, no Context7)`
- idea-intake captured the verbatim raw idea (TODO markers gone), wrote `gate-00.md`, `stage: contextualize`.
- contextualize wrote `## Contextualized`, empty `### BLOCKING`, `gate-01.md`, `stage: market_research`.
- market-research wrote template headings (not `## Market snapshot`), `gate-02.md`, `stage: business_case`.
- business-plan wrote `## Revenue / cost model` + `## Human sign-off` (no `human_approved` key), `gate-03.md`, `stage: tech_architecture`.
- choose-architecture wrote `## Architecture diagram` / `## Stack decisions` (not `## System diagram`), four ADRs, `gate-04.md`, `stage: architecture_rules`.
- No nested Task / other-model builder. Isolation hashes of `harness/skills` and `harness/rules` matched reset.

## Issues found

1. **fixture-limit** — Phases 2–4 market/architecture *judgment* on Chorechip is not a product go/no-go. Oracles graded headings, artifacts, and gate freeze only.
2. **fixture-limit** — No Context7 MCP in this Task; stack pins used web fallback. Version numbers (e.g. Vite 8.2.2) are cheap-model docs retrieval, not a release audit.
3. **isolation-bug (mitigated)** — Cursor Task still has no cwd pin and does not inherit `MIDAS_TRACE_ROOT`. Lab workaround: `{work}/.harness/cache/sandbox-env.json`. Residual: cannot fully close from this repo.

## Proposed improvements (not applied)

1. Cursor product: Task cwd + env pin — cannot close from midas-harness.
2. Optional live `--all` through `define-conventions` + `plan-sprints` (oracles already exist).
3. Optional `/midas-reconcile` Task against `--profile install` now that diagnose is `setup_pending` rather than `partial_migrate`.

## Harness analysis

Capture overlay (`--profile capture`) plus nested vendor install (`--profile install`) close the two fixture gaps from [2026-08-31-smoke](./2026-08-31-smoke.md). Phase 0–4 procedure headings matched the templates the oracles pin. No `harness-gap` from this run — do not extract product rules from cheap-model Chorechip research.

```
MIDAS_SANDBOX_RESULT: skill=idea-intake,contextualize,market-research,business-plan,choose-architecture mode=all verdict=pass auto_decisions=13 isolation=ok
```
