{{!
  TEMPLATE — rendered by /keel-init into the project-root CLAUDE.md.
  This file is a generated shim; the canonical content lives in AGENTS.md.
  Placeholders: none (content is sourced from AGENTS.md.tmpl via @-import).
  Never hand-edit the rendered CLAUDE.md; re-run /keel-doctor to propagate changes.
}}
# Project memory — {{PROJECT_NAME}}

@AGENTS.md

## Claude Code specifics (generated shim — edit AGENTS.md / harness/conventions.md, not this file)

- **Canonical source of truth:** `harness/`, `.claude/skills/`, `AGENTS.md`.
  `CLAUDE.md`, `.cursor/rules/*.mdc`, `.windsurf/rules/*.md` are generated by `/keel-doctor`.
- **Model routing:** orchestrate=`claude-opus-4-8`, build=`claude-sonnet-4-6`, scout=`claude-haiku-4-5`
  (single bump point: `docs/agents-and-models.md`).
- **Current phase:** run `/keel-status` to see the lifecycle state and the single next action.
- **Re-render adapters:** run `/keel-doctor` (or `node scripts/render-adapters.mjs`).
