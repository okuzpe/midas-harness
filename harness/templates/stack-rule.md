<!-- Phase 5 artifact, generated by /define-conventions. ONE stack-specific best-practice rule, filed
     as harness/rules/<slug>.md (e.g. react-hooks.md, postgres-queries.md). The CHECKABLE form of a
     framework/language/DB idiom for THIS project's pinned stack — NOT a slash-command or a skill.
     Generate one per consequential idiom; Context7-verify at the pinned version; never from memory.
     Placeholders: {{RULE}}, {{STACK}}, {{LIB}}, {{VERSION}}, {{TOOL}}. -->

# Rule: {{RULE}} ({{STACK}})

> docs: {{LIB}}@{{VERSION}} via {{TOOL}}
<!-- MANDATORY provenance: the docs this rule was derived from, at the pinned version, via which tool
     (Context7 / web). A generated stack rule WITHOUT this line is a gate FAIL — it may be an idiom
     written from memory rather than current docs. See harness/rules/context7-usage.md. -->

<!-- One short paragraph: the idiom this rule enforces and WHY it matters for {{STACK}}. -->

## Requires

- <!-- the concrete, project-binding constraint(s), e.g. "every DB query is scoped by tenant id" -->

## CHECK

<!-- ≥1 concrete, evidence-based condition the Phase-8 audit grades pass/fail. A grep/command is best;
     `manual:` only when no automated probe exists. Drop the rule if you cannot make it checkable. -->

- **CHECK:** <!-- e.g. `grep -rn "useEffect(" src/ | ...` or `manual: every <X> does <Y>` -->

## Linter form (the machine-readable CHECK)

<!-- Where this rule maps to a lint rule, name the official plugin/rule that enforces it on every commit
     so the CHECK is real BEFORE Phase 8 — e.g. eslint-plugin-react-hooks `exhaustive-deps`,
     @typescript-eslint `no-floating-promises`, eslint-plugin-security, Ruff `S`/`B`. The linter config
     scaffolded in /define-conventions Step 5 must include this plugin. If no lint rule fits, say `manual-only`. -->

- <!-- plugin@rule, or `manual-only` -->
