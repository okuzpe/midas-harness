<!-- Phase 5 artifact, generated by /define-conventions. ONE repeated task, done the project's way.
     Filed as product/playbooks/<verb-noun>.md (e.g. add-api-route.md). A markdown recipe the BUILD
     agent follows during sprints — NOT a slash-command or a generated skill.
     Keep playbooks to the 0–4 tasks that actually recur (zero is valid); drop anything a rule + Context7 covers.
     Reference rules and tokens — never restate them. Placeholders: {{TASK}}, {{STACK}}. -->

# Playbook: {{TASK}}

| Field | Value |
|---|---|
| **Use when** | <!-- the recurring situation, e.g. "adding a new REST endpoint under /api" --> |
| **Trigger** | <!-- a diff PREDICATE meaning this playbook SHOULD have been followed, e.g. "any change under src/app/api/**" or "a new file in db/migrations/**". Phase-8 close-sprint checks: a sprint diff matching this MUST satisfy the done-when below — a match that bypassed the playbook is a fail. --> |
| **Stack** | {{STACK}} <!-- framework@version — Context7-verified --> |
| **Owner tier** | build |

## Steps

<!-- The ordered, project-specific procedure. Each step is concrete and checkable. -->

1. …
2. …
3. …

## Must honor

- **Rules:** <!-- the harness/rules/* this task is bound by (e.g. tenant-isolation, no-cross-layer-imports) — reference, don't restate -->
- **Design tokens:** <!-- product/design-system.md — UI tasks only; never hardcode colour/spacing/type -->
- **Context7:** <!-- fetch <lib>@<version> for the API this touches BEFORE writing code (harness/rules/context7-usage.md) -->

## Done when

<!-- The procedure's OWN completion signal — observable, binary, specific to THIS task (NOT a restatement
     of the rules; that blur is what turns playbooks into a second standards layer). Phase 8 spot-checks these. -->

- [ ] …
- [ ] …
- [ ] All applicable `harness/rules/*` still pass (audited in Phase 8).
